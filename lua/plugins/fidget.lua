return {
    "j-hui/fidget.nvim",
    lazy = "false",
    -- event = "BufEnter",
    opts = {
        notification = {
            -- override_vim_notify = true,
            window = {
                winblend = 0,
            },
        },
        -- logger = {
        --     level = vim.log.levels.INFO,
        -- }
    }
}
